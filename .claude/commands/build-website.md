Use the repository workflow at `.agents/skills/build-website/SKILL.md`.

Read that file completely, then execute its guided intake and full website-build workflow. If `$ARGUMENTS` contains a brief path, use that brief and skip questions already answered there.

Arguments: $ARGUMENTS

Do not stop after generating the brief or plan unless the project uses approval mode. Do not deploy unless explicitly requested.
