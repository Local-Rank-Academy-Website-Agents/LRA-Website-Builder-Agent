import fs from "node:fs";
import http from "node:http";
import path from "node:path";
import process from "node:process";
import { spawn } from "node:child_process";
const cwd=process.cwd(),root=path.join(cwd,fs.existsSync(path.join(cwd,"dist"))?"dist":"site"),config=JSON.parse(fs.readFileSync(path.join(cwd,"site.config.json"),"utf8")),port=Number(process.env.PORT||config.previewPort||4173),url=`http://localhost:${port}`,types={".css":"text/css",".html":"text/html",".js":"text/javascript",".svg":"image/svg+xml",".xml":"application/xml",".txt":"text/plain"};const server=http.createServer((req,res)=>{const pathname=decodeURIComponent(new URL(req.url,`http://${req.headers.host}`).pathname);let file=path.join(root,pathname==="/"?"index.html":pathname);if(fs.existsSync(file)&&fs.statSync(file).isDirectory())file=path.join(file,"index.html");if(!fs.existsSync(file)){res.statusCode=404;file=path.join(root,"404.html")}res.setHeader("Content-Type",types[path.extname(file)]||"application/octet-stream");fs.createReadStream(file).pipe(res)});server.on("error",e=>{if(e.code==="EADDRINUSE"){console.error(`Port ${port} is already in use.`);process.exit(1)}throw e});server.listen(port,()=>{console.log(`Previewing ${root} at ${url}`);if(process.argv.includes("--open")){const c=spawn("cmd.exe",["/c","start","",url],{detached:true,stdio:"ignore",windowsHide:true});c.unref()}});
